import { project } from "..";
import img from "./bulletin_pin.svg";
import markdown from "./bulletin-api-gen.md";
console.log(markdown);
const exp: project = {
  markdown,
  title: "Bulletin Client-Gen Tool",
  image: img,
};

export default exp;
